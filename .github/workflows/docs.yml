name: Docs build and deploy

on:
  workflow_call:
    inputs:
      deploy:
        description: "Deploy docs to nist-page."
        required: false
        type: boolean
        default: false
      no-history:
        description: "Force new commit without parent history."
        required: false
        type: boolean
        default: true

  workflow_dispatch:
    inputs:
      deploy:
        description: "Deploy docs to nist-page."
        required: false
        type: boolean
        default: false
      no-history:
        description: "Force new commit without parent history."
        required: false
        type: boolean
        default: true

env:
  FORCE_COLOR: "3"

jobs:
  build:
    name: Build/deploy docs
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-cached-uv-and-python
        with:
          python-version-file: ".python-version"
          cache-dependency-path: uv.lock

      - name: Build docs
        run: uv run --script noxfile.py -s docs -- ++docs-options -W -T

      - name: Deploy docs
        uses: peaceiris/actions-gh-pages@84c30a85c19949d7eee79c4ff27748b70285e453 # v4.1.0
        if: ${{ inputs.deploy }}
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs/_build/html
          publish_branch: nist-pages
          force_orphan: ${{ inputs.no-history }}
          commit_message: ${{ github.event.head_commit.message }}
