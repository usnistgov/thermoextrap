# Additional tools for development. It is recommended to install these with
# condax/pipx, but you can install them in this environment if you'd prefer.
dependencies:
    - pre-commit
    - tox
    - tox-conda
    - cruft
    - conda-merge
    - scriv
    # this isn't needed, but can be helpful
    # - commitizen
